import type { NextPage } from 'next'
import Head from 'next/head'

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>EBase</title>
        <meta name="EBase" content="EBase" />
      </Head>
    </div>
  )
}

export default Home
